Device TapeAlert SUNW SUNWsamfs - - - /usr/sfw/bin/tapealert_trap "$VENDOR" "$PRODUCT" "$REV" $TOD $EQ_ORD "$NAME" $INQ_TYPE "$VSN" $FLAGS_LEN $FLAGS $VERSION
Device TapeAlert SUNW SUNWsamfs - - - /usr/sfw/bin/tapealert_log "$VENDOR" "$PRODUCT" "$REV" $TOD $EQ_ORD "$NAME" $INQ_TYPE "$VSN" $FLAGS_LEN $FLAGS "$SET" $FSEQ $sequence $VERSION
Archiver - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Releaser - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Stager - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Recycler - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
FS - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Fsd - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Dev - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Misc - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Dump - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Acsls - SUNW SUNWsamfs - - - /usr/sfw/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
