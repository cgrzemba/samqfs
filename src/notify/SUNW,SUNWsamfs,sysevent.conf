Device TapeAlert SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/tapealert_trap "$VENDOR" "$PRODUCT" "$REV" $TOD $EQ_ORD "$NAME" $INQ_TYPE "$VSN" $FLAGS_LEN $FLAGS $VERSION
Device TapeAlert SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/tapealert_log "$VENDOR" "$PRODUCT" "$REV" $TOD $EQ_ORD "$NAME" $INQ_TYPE "$VSN" $FLAGS_LEN $FLAGS "$SET" $FSEQ $sequence $VERSION
Archiver - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Releaser - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Stager - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Recycler - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
FS - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Fsd - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Dev - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Misc - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Dump - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
Acsls - SUNW SUNWsamfs - - - /opt/SUNWsamfs/bin/notify $COMPONENT $SUBCOMPONENT $ERRORTYPE $ERRORID $SYSTEMID "$MESSAGE" "$FAULTTIME" $sequence $ACTION
